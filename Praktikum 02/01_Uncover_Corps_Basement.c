void Uncover_Corps_Basement(char* input)
{
	char* str = "No one is any wiser as another day passes in the dream, and so begins another samsara cycle...";

	if (strings_not_equal(input, str))
		illegal_move();
}